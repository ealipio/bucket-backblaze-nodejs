{
  "name": "@cod3a/uploader",
  "version": "1.0.0",
  "scripts": {
    "build": "pnpm -r run build",
    "dev": "pnpm -r --parallel run dev",
    "start": "node dist/server"
  },
  "description": "monorepo to upload files using multer, back backblaze and nodeJs",
  "author": "Eisson Alipio",
  "license": "ISC",
  "devDependencies": {
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.1.6"
  }
}
